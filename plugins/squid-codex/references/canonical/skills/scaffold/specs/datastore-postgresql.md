<!-- GENERATED — do not edit; run scripts/gen_codex.py -->

---
name: datastore-postgresql
description: PostgreSQL as the project's datastore — SQLAlchemy 2.0 / asyncpg choice, Alembic migrations, connection pooling, `psql` CLI for local dev. TRIGGER when using Postgres. SKIP for document / KV / other datastores.
---

# PostgreSQL

> **Stub — not yet written.** Flesh out as usage patterns reveal opinions worth capturing.

## When to use

- Project's datastore is PostgreSQL.

## When NOT to use

- Project uses a document DB — see `datastore-mongodb.md`.
- Project uses cache / KV — see `datastore-redis.md`.

## Canonical principles

TODO
