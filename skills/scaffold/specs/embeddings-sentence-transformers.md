---
name: embeddings-sentence-transformers
description: Sentence Transformers for local embeddings — model selection, device placement (CPU / CUDA / MPS), batching, quantisation. TRIGGER when running embeddings locally. SKIP for hosted-API embedding stacks.
---

# Sentence Transformers (local embeddings)

> **Stub — not yet written.** Flesh out as usage patterns reveal opinions worth capturing.

## When to use

- Project runs embeddings locally (cost, latency, privacy, offline).

## When NOT to use

- Project uses hosted APIs — see `embeddings-voyageai.md` / `embeddings-openai.md`.

## Canonical principles

TODO
