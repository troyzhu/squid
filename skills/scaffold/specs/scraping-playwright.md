---
name: scraping-playwright
description: Playwright for browser automation — `playwright` Python SDK, async API, browser contexts, screenshots / PDFs, auth flows. TRIGGER when the project drives a real browser. SKIP for static scraping.
---

# Playwright

> **Stub — not yet written.** Flesh out as usage patterns reveal opinions worth capturing.

## When to use

- Project needs JS-rendered pages, auth flows, form submission, or screenshots.

## When NOT to use

- Markdown-ready output via a hosted service — see `scraping-firecrawl.md`.
- Pure static HTML — see `scraping-requests-bs4.md`.

## Canonical principles

TODO
