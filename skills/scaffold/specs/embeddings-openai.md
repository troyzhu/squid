---
name: embeddings-openai
description: OpenAI embeddings via `openai` SDK — `text-embedding-3-large` / `-small`, dimension truncation, batching, cost discipline. TRIGGER when using OpenAI embeddings. SKIP for other embedding providers.
---

# OpenAI embeddings

> **Stub — not yet written.** Flesh out as usage patterns reveal opinions worth capturing.

## When to use

- Project embeds text via OpenAI's `text-embedding-3-*` models.

## When NOT to use

- Project uses Voyage AI — see `embeddings-voyageai.md`.
- Project runs embeddings locally — see `embeddings-sentence-transformers.md`.

## Canonical principles

TODO
