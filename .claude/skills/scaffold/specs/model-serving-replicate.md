---
name: model-serving-replicate
description: Replicate for AI model serving — `replicate` SDK, version pinning, cold-start handling, webhook vs polling. TRIGGER when using Replicate. SKIP for self-hosted / Modal.
---

# Replicate

> **Stub — not yet written.** Flesh out as usage patterns reveal opinions worth capturing.

## When to use

- Project calls pre-packaged or custom models via Replicate's hosted runtime.

## When NOT to use

- Project needs fine-grained control over the serving environment — see `model-serving-modal.md`.

## Canonical principles

TODO
